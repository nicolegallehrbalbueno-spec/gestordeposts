const modal=document.getElementById("postModal"),form=document.getElementById("postForm"),queue=document.getElementById("queue"),titleInput=document.getElementById("postTitle");
const openModal=()=>{modal.classList.add("open");modal.setAttribute("aria-hidden","false");setTimeout(()=>titleInput.focus(),50)};
const closeModal=()=>{modal.classList.remove("open");modal.setAttribute("aria-hidden","true");form.reset()};
document.getElementById("newPost").addEventListener("click",openModal);document.getElementById("firstPost").addEventListener("click",openModal);document.getElementById("closeModal").addEventListener("click",closeModal);document.getElementById("cancelPost").addEventListener("click",closeModal);
modal.addEventListener("click",e=>{if(e.target===modal)closeModal()});document.addEventListener("keydown",e=>{if(e.key==="Escape"&&modal.classList.contains("open"))closeModal()});
form.addEventListener("submit",e=>{e.preventDefault();const title=titleInput.value.trim(),topic=document.getElementById("postTopic").value,date=document.getElementById("postDate").value;const item=document.createElement("div");item.className="postItem";const prettyDate=date?new Date(date+"T12:00:00").toLocaleDateString("pt-BR"):"Sem data";item.innerHTML='<div class="postInfo"><h3></h3><p></p></div><span class="status">Rascunho</span>';item.querySelector("h3").textContent=title;item.querySelector("p").textContent=(topic||"Sem tema")+" · "+prettyDate;if(queue.classList.contains("empty")){queue.className="queueList";queue.innerHTML=""}queue.prepend(item);document.getElementById("draftCount").textContent=document.querySelectorAll(".postItem").length;closeModal();const toast=document.createElement("div");toast.className="toast";toast.textContent="Post salvo como rascunho. ✨";document.body.appendChild(toast);setTimeout(()=>toast.remove(),2600)});